import React from 'react';

const FetchSingleActivity = ({activity}) => {
    console.log(activity.id);
    return <div>chicken</div>
}

//used in FetchActivities to populate the page 
export default FetchSingleActivity;